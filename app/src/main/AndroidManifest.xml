<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.shreyasrana.speaknow3">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--<uses-permission android:name="android.permission.INTERNET" />-->

    <application
        tools:ignore="GoogleAppIndexingWarning"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".wordList" android:exported="true"/>
        <activity android:name=".Suggested" android:exported="true"/>
        <activity android:name=".Name" android:exported="true"/>
        <activity android:name=".scrolling" android:exported="true"/> <!-- <activity android:name=".Name" /> -->
        <!-- <activity android:name=".Pictures" /> -->
        <!-- <activity android:name=".SpellOut" /> -->
        <activity
            android:name=".abc" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
